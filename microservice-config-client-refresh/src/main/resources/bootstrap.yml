spring:
  cloud:
    config:
      uri: http://localhost:8100
      profile: dev
      label: master
  application:
    name: user
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest


